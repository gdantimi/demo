{
  "name": "App",
  "engines": {
    "node": "9.3.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "@angular/cli": "1.3.1",
    "@angular/compiler-cli": "^4.2.4",
    "typescript": "~2.3.3"
  }
}
